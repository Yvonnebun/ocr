pdf2image>=1.16.3
Pillow>=10.0.0
requests>=2.28.0
pymupdf>=1.23.0
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
shapely>=2.0.0
ultralytics>=8.0.0

# Note: layoutparser and detectron2 are no longer needed locally
# Layout detection is done via HTTP call to layout-service (Linux/Docker)
